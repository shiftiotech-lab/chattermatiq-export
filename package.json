{
  "name": "chattermatiq-export",
  "version": "0.1.0",
  "description": "ChattermatIQ — export & analyze YouTube comments (real, via YouTube Data API).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "fastify": "^5.12.0"
  }
}
